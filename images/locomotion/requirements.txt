# Python dependencies for production locomotion container
# Base image (nvcr.io/nvidia/pytorch:25.10-py3-igpu) already includes:
# - PyTorch 2.9.0a0+145a3a7
# - NumPy 1.26.4?
# - CUDA 9.0
# - cuDNN 9.10.2.21
# - TensorRT 10.11.0.33+jp6
# - Python 3.11?

# ZMQ communication (inproc for internal, TCP for external if needed)
pyzmq>=25.0.0

# Data handling
dataclasses-json>=0.6.0

# Utilities
typing-extensions>=4.8.0

# Scientific computing (required for image resizing in hal_server)
scipy>=1.10.0

# Reinforcement learning library
rsl-rl-lib>=2.3.1

# ZED SDK Python bindings (requires ZED SDK to be installed on system)
# Note: ZED SDK must be installed separately via NVIDIA's installer
# If ZED SDK is not available, the code will gracefully fall back to mock mode
pyzed>=3.8.0

# Gamepad E2E (controller/scripts/jetson client)
pygame>=2.6.0
PyYAML>=6.0.0

# MCU serial (firmware.krabby_mcu)
pyserial>=3.5
keyboard>=0.13.0

# MaixSense HTTP RGB-D support (decode + transport)
requests>=2.31.0
opencv-python-headless>=4.8.0

