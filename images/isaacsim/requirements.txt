# Python dependencies for Isaac Sim container
# Base image (nvcr.io/nvidia/isaac-sim:5.1.0) already includes:
# - Isaac Sim 5.1.0 (Kit 107.3.3)
# - Omni
# - PyTorch (usually pre-installed)
# - NumPy (usually pre-installed)

# ZMQ communication (inproc for internal communication)
pyzmq>=25.0.0

# Data handling
dataclasses-json>=0.6.0

# Utilities
# typing-extensions>=4.10.0 required for PyTorch 2.7.0 compatibility
typing-extensions>=4.10.0
# flatdict required by isaaclab.sim.simulation_context
flatdict
# matplotlib>=3.5.0 required for Python 3.12 compatibility (older versions use deprecated configparser.SafeConfigParser)
matplotlib>=3.5.0

# Parkour terrain generation (required for mesh simplification)
fast-simplification

# Testing
pytest>=7.0.0

# Reinforcement learning library
# Note: Isaac Lab installs rsl-rl-lib 3.1.2 as a dependency, which overrides this
# Version 3.1.2 has EmpiricalNormalization in rsl_rl.networks
# Version 2.3.1 has EmpiricalNormalization in rsl_rl.modules
# This pin is for documentation - actual version will be 3.1.2 from Isaac Lab
rsl-rl-lib==3.1.2

# HAL data collector (Python settings + rosbag2 mcap writer); pure Python, no ROS distro required
PyYAML>=6.0.0
rosbags>=0.10.0

# Note: scipy is NOT installed here - Isaac Sim has its own bundled scipy
# Installing scipy in the venv causes conflicts with Isaac Sim's bundled scipy

