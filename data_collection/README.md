# krabby-data-collection

HAL data collection: ROS 2 rosbag2 (mcap) recording from `HardwareObservations` via a second `HalClient`.

Built as a wheel with `make build-wheels` (output under `data_collection/dist/`).
