# `krabby-teleop-portal` (operator server)

Wheel root: **`teleop/portal/`**.

Operator-facing aiohttp app (no **aiortc**). Serves the viewer UI + relays signaling between browser and robot.

Public import namespace: **`teleop.portal`**.

