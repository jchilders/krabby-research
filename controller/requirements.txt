# Third-party dependencies for krabby-controller (matches pyproject.toml).
# Install krabby-hal-client from repo or PyPI: pip install -e hal/client (or pip install krabby-hal-client).
pygame>=2.5.0
pyzmq>=25.0.0
